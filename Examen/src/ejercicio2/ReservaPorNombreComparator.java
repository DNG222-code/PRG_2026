package ejercicio2;

// Importamos todos los pakages de util.
import java.util.*;

public class ReservaPorNombreComparator implements Comparator<Reserva> {

    // Implementamos el compare
    @Override
    public int compare(Reserva o1, Reserva o2) {
        return 0;
    }
}
